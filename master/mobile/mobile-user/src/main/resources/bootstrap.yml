spring:
  application:
    name: mobile-user #设置服务的名称

  profiles:
    active: @spring.acthttp://localhost:9767/mobile-user/user/test/ive@

  cloud:
    config:
      profile: @spring.active@
      name: mobile-user   #文件前缀名称  #git 文件的环境   例如 {name}-{profile} = service-gateway-dev.yml
      enabled: true
      uri:
        - http://127.0.0.1:9762
        - http://127.0.0.1:9760
